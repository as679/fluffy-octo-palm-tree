input {
  udp {
    host => "localhost"
    port => 10514
    codec => "json"
    type => "rsyslog"
  }
  snmptrap {
    port => 162
    type => "snmptrap"
  }
}

filter { }

output {
  if [type] == "rsyslog" or [type] == "snmptrap" {
    elasticsearch {
      hosts => [ "localhost:9200" ]
    }
  }
}